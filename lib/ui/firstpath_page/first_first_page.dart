import 'package:flutter/material.dart';

class FirstFirstPage extends StatelessWidget {
  const FirstFirstPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("firstfirstpage"),),
      body: Placeholder(),
    );
  }
}