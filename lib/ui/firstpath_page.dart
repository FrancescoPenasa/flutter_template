import 'package:flutter/material.dart';


class FirstPathPage extends StatelessWidget {
  const FirstPathPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("firstPath"),),
      body: Placeholder(),
    );
  }
}
