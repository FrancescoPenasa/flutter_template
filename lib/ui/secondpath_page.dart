import 'package:flutter/material.dart';


class SecondPathPage extends StatelessWidget {
  const SecondPathPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("secondpath"),),
      body: Placeholder(),
    );
  }
}
